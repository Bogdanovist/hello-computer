#pragma once

// whisper.h must be on the include path.
// Build with: swift build -Xcc -I/usr/local/include -Xlinker -L/usr/local/lib
// The install script (scripts/install.sh) ensures whisper.cpp is built and installed.
#include "whisper.h"
